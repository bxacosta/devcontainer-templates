[tool.poetry]
name = "python-flask-poetry"
version = "0.1.0"
description = ""
authors = ["Bryan Acosta <bxacosta@hotmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
