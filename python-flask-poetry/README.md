# Python Flask Server

This project offers a minimal setup for running a [Flask](https://flask.palletsprojects.com/en/stable/) server
with [Poetry](https://python-poetry.org/) as the dependency manager.

## Features

- Flask
- Poetry

## How to Run

Install Poetry and in this directory, run:

```bash
poetry install
poetry run flask run
```