# Java + Gradle

This is a basic project configured with Java 21 and Gradle

## Features

- Java 21
- Gradle
- JUnit

## Build and run

Use Gradle to build and run the project:

```bash
./gradlew build
./gradlew run
```